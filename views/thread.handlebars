<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/public/css/style.css">
  <title>{{op.name}}'s Thread</title>
</head>
  <body>
    <div class="topnav">
      <a href="/home">Home</a>
      <a href="/books">Books</a>
      <a href="/users">Users</a>
      <a class="active" href="/groups">Groups</a>
      <a href="/tags">Tags</a>
      <a href="/lists">Lists</a>
      <div class="topnav-right">
        <a href="/logout">Logout</a>
      </div>
    </div>
    <br>
    <a href="/groups">Return to Group Listings</a>
    <h2>{{body.initialComment}}</h2>
    <p>Posted by: {{op.name}}</p>
    <form method="POST" action="/threads/addComment/{{body._id}}">
      <label for="threadComment">Add a new Comment!</label>
      <input id="threadComment" type="text" name="threadComment">
      <button type="submit">Click to add a comment!</button>
    </form>
    {{#if body.comments.length}}
          {{#each body.comments}}
            <div class="comment">
              <ul class="no-bullets">
                <li>User: <a href="/users/{{this.commenter}}">{{this.name}}</a><li>
                <li>Comment: <br>&ensp;{{this.text}}</li>
                <li>Date: {{this.when}}</li>
              </ul>
            </div>
          {{/each}}
    {{else}}
      <p>There are no comments on this thread.</p>
    {{/if}}
  </body>
</html>
